<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorFondo"
    tools:context=".PlaylistsPublicasActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Campo de búsqueda -->
        <EditText
            android:id="@+id/campoBusqueda"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Buscar playlists públicas"
            android:inputType="text"
            android:textColor="@color/colorTexto"
            android:textColorHint="@color/colorGris"
            android:backgroundTint="@color/colorGris"
            android:drawableStart="@drawable/ic_search"
            android:padding="12dp"
            android:layout_marginBottom="16dp" />

        <!-- RecyclerView para mostrar las playlists -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerPlaylistsPublicas"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@android:color/transparent" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
